import Sidebar from "@/components/general/sidebar";

const page = () => {
  return (
    <div className="flex w-full">
      <Sidebar />
      <div className="w-full"></div>
    </div>
  );
};

export default page;
